import React from "react"
import { FileIcon, FolderIcon, SaveIcon } from "../Icons"

import "./styles.css";

export default ({
}) => (
  <div className="toolbar panel">
    <FileIcon />
    <FolderIcon />
    <SaveIcon />
  </div>
)
